export function useControl() {
    // const configuration = useContext(ConfigurationContext);
    // const dispatch = useDispatch();
    // const control = useRef<ControlService>(new ControlService(dispatch, SnapStream.getClientId()));
    //
    // useEffect(() => {
    //     const connection = control.current;
    //     connection.disconnectSafe()
    //     connection.connect(`ws://${configuration.host}:${configuration.websocket.port}${configuration.websocket.path}`);
    //     return () => {
    //         connection.disconnectSafe();
    //     }
    // }, [configuration.host, configuration.websocket]);
    //
    // return {control: control.current};
}